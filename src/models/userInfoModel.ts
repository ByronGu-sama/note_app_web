export interface IUserInfo {
    uid?: number,
    username?: string,
    avatarUrl?: string,
    birth?: string,
    gender?: string,
    signature?: string,
    address?: string,
    language?: string,
    collects?: number,
    follows?: number,
    followers?: number,
    likes?: number,
    noteCount?: number,
}