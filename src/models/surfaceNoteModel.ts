export interface ISurfaceNote {
    title?: string; // 标题
    cover?: string; // 封面
    coverHeight?: number; // 封面高度
    nid?: number;
    author?: string; // 作者
    authorAvatar?: string; // 作者头像
    likes?: number; // 点赞数
}